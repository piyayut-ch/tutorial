---
output: html_document
---

# Initialize

Generate a blog using `create_blog()` function
```{r, eval = FALSE}
library(distill)
create_blog(dir = "", title = "")
```

# Configuration
ปรับแต่ง website ในไฟล์ `_site.yml`

```{r, eval = FALSE}

```
